"use client";

// Thin re-export: the two hooks below were hand-rolled over @tanstack/virtual-core,
// which duplicates what @tanstack/react-virtual already ships.
export { useVirtualizer, useWindowVirtualizer } from "@tanstack/react-virtual";
